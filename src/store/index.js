import { createStore } from 'vuex'
import EventModule from './event/index'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    event: EventModule
  }
})
